# Correr el proyecto usando los comandos que hay en package.json

Con npm run dev se levanta el servidor de desarrollo
Con npm run build se genera dist, listo para la distribucion final en el servidor
